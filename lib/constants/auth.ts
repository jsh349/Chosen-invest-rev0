/**
 * Placeholder user ID for local-first mode.
 * Replace with real auth user ID when API integration is added.
 */
// TODO: API Migration — all stored entities (Asset, Goal, Transaction, HouseholdMember)
// use this ID. When real auth is added, a migration is required to reassign these
// records to the authenticated user's real ID. Do not remove this constant until migration is complete.
export const LOCAL_USER_ID = 'local_user'
